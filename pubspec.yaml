name: net_mock
description: A Dart package designed to simplify HTTP request mocking in your tests.
version: 1.0.6
repository: https://github.com/KwabenBerko/net_mock

environment:
  sdk: ^3.4.3

dependencies:
  equatable: ^2.0.5
  http: ^1.2.2
  http_interceptor: ^2.0.0

dev_dependencies:
  lints: ^3.0.0
  test: ^1.24.0
